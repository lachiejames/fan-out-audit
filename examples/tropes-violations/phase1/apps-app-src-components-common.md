# Tropes Audit: apps/app/src/components/common

Files audited:

- PlatformIcon.tsx

---

## No findings

`PlatformIcon.tsx` is a pure presentational utility component. It resolves a platform ID string to an SVG icon component and renders it at a configurable size, falling back to a generic circle icon. There is no user-facing copy and no AI trope language.
